  <h1><%= @sponsoredpost.name %></h1>

 <%= link_to "Edit Post", edit_sponsoredpost_path, class: 'btn btn-success' %>
 <div class="row">
   <div class="col-md-8">
     <p class="lead"><%= @sponsoredpost.description %></p>
     <% @sponsoredpost.posts.each do |post| %>
       <div class="media">
         <div class="media-body">
           <h4 class="media-heading">
 <!-- #39 -->
             <%= link_to post.title, post %>
             <%= link_to post.title, topic_post_path(@sponsoredpost, post) %>
           </h4>
         </div>
       </div>
     <% end %>
   </div>
   <div class="col-md-4">
 // #41
     <%= link_to "New Sponsored Post", new_sponsoredpost_path(@sponsoredpost), class: 'btn btn-success' %>
   </div>
 </div>
